[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
version = "0.1.0"
name = "jobscriptgenerator"
requires-python = ">= 3.10"

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]